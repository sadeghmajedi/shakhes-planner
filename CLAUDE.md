# CLAUDE.md

راهنمای این ریپازیتوری برای Claude Code. این یک اپ تک‌صفحه‌ای (single-file PWA) بدون build step یا وابستگی npm است — همه‌چیز در `index.html` نوشته و مستقیماً در مرورگر اجرا می‌شود.

## ۱. معماری اپ

### ساختار فایل‌ها
```
index.html      تمام markup + CSS + منطق JS اپ (تک فایل، ~۲۰۰۰ خط)
sw.js           Service Worker (کش‌گذاری offline)
manifest.json   PWA manifest (آیکون‌ها، نام، تم رنگی)
version.json    {"version": "...", "date": "..."} — برای چک کردن آپدیت از سمت سرور
icon.svg
apple-touch-icon.png
```
هیچ bundler، framework یا package manager‌ای در کار نیست. ویرایش‌ها مستقیماً روی همین فایل‌ها انجام می‌شود.

### کلیدهای localStorage
| کلید | محتوا |
|---|---|
| `shakhes_tasks` | آرایه‌ی JSON تمام تسک‌ها (منبع اصلی داده‌ی کاربر) |
| `shakhes_grp_order` | ترتیب گروه‌ها |
| `shakhes_stats_history` | تاریخچه‌ی آمار روزانه (برای نمودار/گزارش پیشرفت) |
| `shakhes_last_check_date` | آخرین تاریخی که ریست خودکار روزانه (auto-reset تسک‌های دوره‌ای) اجرا شده |
| `shakhes_pin` | هش/مقدار رمز عبور ۴ رقمی قفل اپ |
| `shakhes_pin_enabled` | `'1'` یا `'0'` — روشن/خاموش بودن قفل با رمز |

خروجی/ورودی پشتیبان‌گیری (backup JSON که کاربر دانلود یا restore می‌کند) شامل `shakhes_tasks` و `shakhes_grp_order` به‌همراه یک `backupVersion` عددی است (نسخه‌ی فرمت فایل پشتیبان، **جدا** از نسخه‌ی اپ — این را با CURRENT_VERSION اشتباه نگیر).

### استراتژی کش (sw.js)
- **Cache name**: `shakhes-planner-v<VERSION>` — با هر بمپ نسخه، نام کش هم عوض می‌شود تا نصب جدید کش قبلی را دور بیندازد (در `activate` هر کش با نام قدیمی پاک می‌شود).
- **درخواست‌های ناوبری/HTML** (`navigate` یا `destination === 'document'`): **network-first** — همیشه اول از شبکه می‌گیرد و فقط اگر آفلاین بود از کش برمی‌گردد. این باعث می‌شود آپدیت HTML سریع دیده شود.
- **همه‌ی درخواست‌های دیگر** (آیکون، manifest، فونت و ...): **cache-first** — اگر در کش بود همان را می‌دهد، وگرنه از شبکه می‌گیرد و کش می‌کند.
- `self.skipWaiting()` در `install` و `self.clients.claim()` در `activate` صدا زده می‌شوند، یعنی SW جدید بلافاصله (بدون نیاز به بستن همه‌ی تب‌ها) کنترل صفحه را می‌گیرد.
- `version.json` **در `FILES` کش install قرار ندارد** — عمداً، چون باید همیشه مستقیم از شبکه با `?t=timestamp` خوانده شود تا مکانیزم چک‌آپدیت (`checkForUpdate` در index.html) گول کش نخورد.

## ۲. جاهایی که شماره‌ی نسخه وجود دارد

نسخه‌ی اپ در **سه جای مجزا** نگه‌داری می‌شود و باید همیشه با هم و همزمان آپدیت شوند، وگرنه مکانیزم تشخیص آپدیت و کش خراب می‌شود:

1. **`index.html`** — متغیر `var CURRENT_VERSION = '1.11.3';` (نزدیک تابع `checkForUpdate`). این عدد داخل خودِ اپِ لود شده در دستگاه کاربر است و با نسخه‌ی موجود در `version.json` مقایسه می‌شود.
2. **`sw.js`** — `const CACHE = 'shakhes-planner-v1.11.3';` در خط اول فایل. این نام کش است؛ اگر عوض نشود، SW جدید هم فکر می‌کند کش قبلی هنوز معتبر است.
3. **`version.json`** — فیلد `"version"`. این فایلی است که `checkForUpdate()` هر بار با `fetch('version.json?t=' + Date.now())` می‌گیرد تا بفهمد نسخه‌ی جدیدتری روی سرور هست یا نه.

**قانون**: هر بار که نسخه بالا می‌رود (حتی یک patch)، هر سه‌ی این‌ها باید با هم و به یک مقدار یکسان آپدیت شوند. فراموش کردن حتی یکی از این سه یعنی:
- اگر `version.json` عقب بماند → کاربرها هیچ‌وقت اعلان آپدیت نمی‌بینند.
- اگر `CACHE` در `sw.js` عقب بماند → فایل‌های جدید هیچ‌وقت واقعاً جایگزین کش قدیمی نمی‌شوند.
- اگر `CURRENT_VERSION` در `index.html` عقب بماند → مقایسه‌ی نسخه در `checkForUpdate` نادرست می‌شود.

## ۳. قانون Service Worker در iOS: `updateViaCache: 'none'`

ثبت Service Worker (در انتهای `index.html`) **همیشه** باید با گزینه‌ی `updateViaCache: 'none'` انجام شود:

```js
navigator.serviceWorker.register('./sw.js', { updateViaCache: 'none' });
```

**چرا**: بدون این گزینه، مرورگر (به‌خصوص Safari/iOS) خودِ فایل `sw.js` را طبق HTTP cache headers معمولی کش می‌کند. یعنی حتی وقتی محتوای `sw.js` روی سرور عوض شده، مرورگر ممکن است نسخه‌ی کهنه‌ی همان فایل را از کش HTTP بردارد و اصلاً متوجه تغییر نشود — در نتیجه منطق جدید کش/آپدیت هیچ‌وقت دانلود و اجرا نمی‌شود، حتی با فشار دادن دکمه‌ی «بررسی آپدیت». این تنظیم به مرورگر می‌گوید فایل SW را همیشه مستقیم از شبکه (و نه از HTTP cache) بگیرد.

یک listener برای رویداد `controllerchange` هم باید وجود داشته باشد تا وقتی SW جدید کنترل صفحه را گرفت، صفحه یک‌بار (با گارد ضد حلقه) reload شود.

## ۴. محدودیت مهم iOS: جدایی کامل فضای PWA نصب‌شده و Safari

روی iOS، **اپ نصب‌شده روی صفحه‌ی هوم (Add to Home Screen) و برگه‌ی همان سایت در Safari دو origin/context کاملاً مجزا از نظر ذخیره‌سازی هستند**:
- `localStorage` بین این دو مشترک نیست — تسک‌های ذخیره‌شده در PWA نصب‌شده در Safari دیده نمی‌شوند و برعکس.
- ثبت Service Worker و کش آن هم مستقل است — SW نصب‌شده در یکی، در دیگری وجود ندارد.

**پیامد حیاتی**: پاک کردن (حذف) اپ از صفحه‌ی هوم و نصب دوباره، یعنی از دست رفتن کامل `shakhes_tasks` و بقیه‌ی داده‌های کاربر که فقط در همان context نصب‌شده ذخیره شده بودند. Safari هیچ نسخه‌ی پشتیبانی از این داده‌ها ندارد.

⚠️ **هرگز** «حذف و نصب مجدد اپ» را به‌عنوان راه‌حل رفع مشکل (مثلاً برای رفع مشکل آپدیت یا کش) به کاربر پیشنهاد نده. تنها راه امن برای رفع مشکلات کش/SW در همان context نصب‌شده است (مثلاً دکمه‌ی «بررسی آپدیت» که رجیستریشن‌های SW را unregister و کش‌ها را پاک می‌کند ولی `localStorage` را دست‌نخورده نگه می‌دارد — به تابع `doUpdate()` نگاه کن). اگر کاربر می‌خواهد داده‌هایش حفظ شود، همیشه اول پیشنهاد گرفتن پشتیبان (Backup → دانلود فایل JSON) را بده.

## ۵. قوانین کار روی این ریپو

- **ویرایش هدفمند، نه بازنویسی کامل**: چون همه‌چیز در یک `index.html` بزرگ است، وسوسه‌ی rewrite کردن کل فایل یا بخش‌های بزرگ آن زیاد است — مقاومت کن. فقط خطوط/تابع مرتبط با تغییر را ویرایش کن (با ابزار diff/edit، نه بازنویسی کامل فایل)، تا ریسک شکستن منطق دیگر (drag & drop، محاسبات تاریخ شمسی، PIN، backup/restore و ...) کم بماند.
- **قبل از commit، جاوااسکریپت را validate کن**: چون تست خودکار (CI/lint) در این ریپو وجود ندارد، حداقل با `node --check` روی اسکریپت استخراج‌شده یا با باز کردن فایل در مرورگر و چک کردن console مطمئن شو که سینتکس/اجرای اولیه بدون خطاست. مثال سریع:
  ```bash
  node -e "new Function(require('fs').readFileSync('index.html','utf8').match(/<script>([\s\S]*?)<\/script>/)[1])"
  ```
- **حتماً روی دستگاه واقعی تست کن، نه فقط دسکتاپ**: این اپ یک PWA با هدف اصلی iOS/موبایل است (نصب روی صفحه‌ی هوم، swipe gestures، PIN، رفتار Service Worker). خیلی از باگ‌های حیاتی (مثل کش نشدن sw.js، جدا بودن storage نصب‌شده) فقط روی دستگاه واقعی و در حالت PWA نصب‌شده قابل مشاهده‌اند، نه در دسکتاپ Chrome. تست دسکتاپ برای تغییرات UI/منطق کلی کافی است ولی برای هر تغییری که به Service Worker، کش، یا storage مربوط می‌شود کافی نیست.

## ۶. نکته‌ی گاردی: برچسب نسخه‌ی hardcode در UI

در پروژه‌های مشابه (مثلاً PFM) باگی دیده شده که شماره‌ی نسخه به‌صورت متن ثابت (hardcode) در markup/UI نوشته شده بود و در runtime هیچ‌وقت با `CURRENT_VERSION` یا `version.json` همگام نمی‌شد — یعنی حتی بعد از آپدیت واقعی، کاربر همچنان نسخه‌ی قدیمی را در صفحه می‌دید.

در این ریپو (`shakhes-planner`) در حال حاضر چنین موردی **دیده نشد**: تنها جایی که نسخه در UI نمایش داده می‌شود عنصر `#updVersionInfo` است که همیشه به‌صورت دینامیک با `CURRENT_VERSION` پر می‌شود (`checkForUpdate()` و `openBackupModal()` در `index.html`). متن «نسخه جدید» روی دکمه‌ی FAB صرفاً یک برچسب ثابت است و شامل عدد نسخه نیست.

با این حال، **هر بار قبل از commit یک تغییر مرتبط با نسخه، این نکته را دوباره چک کن**: در `index.html` دنبال هر رشته‌ی متنی حاوی الگوی `\d+\.\d+\.\d+` یا نوشتن دستی «نسخه X.Y.Z» بگرد که مستقیم در HTML/JS نوشته شده باشد و از متغیر `CURRENT_VERSION` نیاید — اگر پیدا شد، یعنی باگ مشابه PFM رخ داده و باید به‌جایش از `CURRENT_VERSION` استفاده شود.
